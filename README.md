# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

##### Данные
Интерфейсы используемые в приложении и классах:

1) IProduct описывает сущность товар/продукт. 
Нужен для задания типа у хранилищ товаров в экземпляре класса корзины и каталога. Так же хранилище для выбранной карточки в экземпляре каталога будет иметь этот тип. 

Interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

2) IBuyer описывает сущность покупателя.
Нужен для возращения информации о покупателе в нужно ввиде.

Interface IBuyer {
  payment: 'card'|'cash'|''
  address: string
  email: string
  phone: string
}

3) IGet - описывает ответ сервера на запрос товаров.
interface IGet {
  total: number,
  items: IProduct[]
}

4) IBodyOrder - описывает тело запроса заказа на сервер.
Нужен для отправки заказа на сервер(POST). Тело POST состоит из информации о покупателе,массив id товаров и их общая цена.
Наследует интерфейс IBuyer.

interface IBodyOrder extends IBuyer {
  total: number,
  items: string[]
}

5) IPost - описывает обьект ответа сервера на запрос POST. 

interface IPost {
  id: string,
  total: number
}

// интерфейсы используемые в классах представлениях

6) IHeader - описывает класс представления Header. Нужен для изменения разметки по определённым полям.
interface IHeader {
  counter: number
}

7) IGallery  - описывает класс представления Gallery. Нужен для отрисовки товаров внутри Gallery.

interface IGallery {
  items:HTMLElement[]
}

8) IModal - описывает класс представления Modal. Нужен для изменения контента внутри модального окна.

interface IModal {
  content: HTMLElement
}

9) IOrderSuccess - описывает класс представления OrderSuccess. Нужен для отрисовки итогового списания суммы.

interface IOrderSuccess {
  writenOff: number;
}

10) ICardCatalog - описывает  класс представления CardCatalog.

type ICardCatalog = Pick<IProduct,'category'|'image'|'price'|'title'>

11) ICardActions - описывает обьект с методом onclick нужным для получением данных,но не хранить их.

type ICardActions = {
  onclick:()=> void;
}

12) ICardFull - описывает класс представления CardFull. Берёт нужные поля из тип IProduct. Показывает какие данные могут рендериться.

buttonText - отвечает за текст на кнопке.(Купить|Удалить из корзины|Недоступно)

type ICardFull = Pick<IProduct,'category'|'description'|'image'|'price'|'title'> & {buttonText:string}

13) ICardBasket - описывает класс представления CardBasket. Берёт нужные поля из тип IProduct. оказывает какие данные могут рендериться.

index - отвечает за номер товара в корзине.

type ICardBasket = Pick<IProduct,'price'|'title'> & {index:number}

###### Модели данных

####### Класс Catalog
Класс используемый для хранения и работы с товарами на главной странице.
Хранит в виде массива товары доступные на странице и отдельно выбранный товар пользователем.

Конструктор:  
`constructor(products: IProduct[])` - в конструктор передаётся массив товаров.

Поля класса:  
`_products: IProduct[]` - хранит товары доступные на главной странице
`_productCard: IProduct|undefined` - хранит товар для того чтобы показать подробную информацию

Методы класса:
`setItems(products: IProduct[])` - сохраненяет товары в поле _products, принимает массив товаров.
`getItems(): IProduct[]`  - получает список товаров 
`setProductCard(productCard: IProduct)` - сохраняет выбранную карточку,принимает карточку которую выбрал пользователь.
`getProductCard(): IProduct` - возращает сохранённую карточку для подробного отображения.
`getProductById(id: string):IProduct` - получает товар по id,принимает id.

####### Класс ShoppingBasket
Класс используемый для хранения товаров в корзине,а так же работы с этими товарами.

Поля класса:
`_products: IProduct[]` - хранит товары корзины

`constructor(eventBroker: IEvents)`

Методы класса:
`addProduct(product: IProduct)` - добавляет товар в массив товаров корзины,принимает товар который нужно добавить в массив товаров корзины.
`deleteProduct(product: IProduct)` - удаляет товар из массива товаров корзины,принимает товар который нужно удалить.
`getLength(): number` - возращает количество товаров в корзине
`getProducts(): IProducts[]` - возращает список товаров из корзины.
`getPrice(): number` - возращает стоимость всех товаров в корзине
`checkProduct(productId: string):boolean` - проверяет есть ли товар в корзине,принимает id товара и возращает true если есть и false если нет.
`clearAll()` - очистка массива товаров корзины.

####### Класс Buyer
Класс используемый для хранения информации о покупателе который должен указать при оформлении заказа.

Поля класса:
`_payment: "card"|"cash"|""` - хранит способ оплаты покупателя.
`_address: string` - хранит адрес покупателя.
`_email: string` - хранит почту покупателя.
`_phone: string` - хранит телефон покупателя.

Методы класса:
`setPayment(payment: "card"|"cash"|"")` - устанавливает вид оплаты у пользователя.
`setAddress(address:string)` - устанавливает новый адресс у пользователя.
`setEmail(email:string)` - устанавливает новую почту у пользователя.
`setPhone(phone:string)` - устанавливает новый телефон у пользователя.
`getAllInformation(): IBuyer` - возращает информацию о пользователе.
`clearAllInformation()` - очищает всю информацию о пользователе.
`validatePayment():string` - валидация выбора оплаты,если пустая то успешно.
`validateAddress():string` - валидация адреса,если пустая то успешно.
`validateEmail():string` - валидация выбора оплаты,если пустая то успешно.
`validatePhone():string` - валидация выбора оплаты,если пустая то успешно.

###### Слой коммуникации

####### Класс ApiService

Этот класс будет использовать композицию,
чтобы выполнить запрос на сервер с помощью методов get,post класса Api
и будет получать с сервера объект с массивом товаров,отправлять заказ на сервер.

`constructor(api: Api)` - конструктор будет принимать экземпляр класса api  для запросов к серверу.

Поля класса:

`api: Api` - экземпляр класса Api содержит в себе базовую логику отправки запросов.

Методы класса:

`getProducts(): Promise<IGetProducts>` - запрос GET товаров у сервера.
`sendOrder(body:IBodyOrder): Promise<ISendOrder> ` - запрос POST на сервер.
Принимает обьект типа IBodyOrder. Использует метод post экземпляра класса api.

###### Классы представления(View)

####### Класс Header

Класс отвечает за отрисовку в шапке. Именно он будет перерисовывать счётчик количества товара над корзиной при добавлении/удалении товара в корзине.

Конструктор:
`constructor(container: HTMLElement, eventBroker: IEvents)` - конструктор будет принимать ссылку на DOM элемент шапки и брокер событий. В разметке находим кнопку открывающую корзину, элемент отвечающий за количество товаров в корзине.

Поля класса:
`basketButton: HTMLButtonElement` - кнопка в разметке отвечающая за открытие корзины.
`counterElement: HTMLElement` - элемент разметки отвечающий за отрисовку количества товаров в корзине.
`eventBroker: IEvents ` - хранит ссылку на класс брокера событий с помощью которого класс представления сообщает презентуру что пользователь что то сделал.

Методы класса:
`set counter(counter: number)` - сеттер смены количества товара на кнопке открывающей корзину.

####### Класс Gallery

Класс отвечает за разметку галлерии на главной странице

`constructor(container: HTMLElement)` - конструктор принимает ссылку на DOM отвечающую за разметку внутри каталога на главной странице. Сам контейнер будет местом куда мы добавим карточки на главной странице.

Поля класса:
`catalogElement: HTMLElement` - место в разметке для карточек.

Методы класса:
`set items(products: HTMLElement[])` - сеттер смены списка товаров в каталоге. Принимает массив элементов разметки товаров.

####### Класс Modal

Класс отвечает за разметку модального окна.

`constructor(container: HTMLElement,eventBroker: IEvents)` - принимает ссылку на DOM элемент модального окна и брокера событий.

Поля класса:
`modalButtonClose: HTMLButtonElement` - кнопка при нажатии на которую закроется модальное окно.
`modalContent: HTMLElement` - поле для ссылки чтобы в дальнейшем сюда добавлять контент.
`modalContainer: HTMLElemnt` - поле для хранения контейнера модального окна и остановки всплытия по клику(чтобы не закрывать окно как по оверлею). 
`eventBroker: IEvents` - поле для хранения ссылки на брокер событий

Методы Класса:
`set content(content: HTMLElement)` - cеттер для установки контента в поле modalContent. При срабатывании открывается модальное окно путём вызова метода OpenModal().

`openModal():void` - метод для открытия модального окна. Навешивается класс на контейнер modal_active.

`closeModal():void` - метод для закрытия модального окна. Класс modal_active убирается с контейнера.

####### Класс OrderSuccess

Класс отвечает за разметку успешного заказа.

`constructor(container: HTMLElement, eventBroker: IEvents)` - конструктор принимает ссылку на DOM отвечающую за разметку успешного заказа и брокер событий. В разметке контейнера найдём элемент отвечающий за списанную итоговую цену и кнопку возращения на главную страницу.

Поля класса:
`orderSuccessWrittenOFF: HTMLElement` - место в разметке отвечающее за списанную сумму в успешном заказе.
`orderSuccessButton: HTMLButtonElement` - поле хранения ссылки на кнопку возращения не главную страницу.
`eventBroker: IEvents` - ссылка на брокер событий.
Методы класса:
`set writtenOff(writenOff: number)` - метод отвечающий за смену итоговой стоимости в успешном заказе.

####### Класс Card

Класс является базовым для данных классов:

1)CardCatalog
2)CardFull
3)CardBasket

`constructor(container: HTMLElement)` - конструктор принимает ссылку на DOM. В контейнере найдём разметку заголовка и ценника.

Поля класса:
`cardTitle: HTMLElement` - поле для хранения разметки заголовка 
`cardPrice: HTMLElement` - поле для хранения разметки ценника

Методы класса:
`set title(cardTitle: string)` - метод для установки новой разметки в поле cardTitle
`set price(cardPrice: number)`- метод для установки новой разметки в поле cardPrice.

####### Класс CardCatalog

Класс отвечает за разметку карточки из каталога.

`constructor(container: HTMLElement, actions?: ICardActions)` - конструктор принимает ссылку на DOM разметку карточки в каталоге и обьект с методом onclick для того чтобы навести обработчик при нажатии на карточку. 

Поля класса:
`cardCategory: HTMLElement` - поле для хранения разметки заголовка.
`cardImage: HTMLElement` - поле для хранения разметки ценника.
`cardButton: HTMLElement` - поле для хранения самого контейнера.

Методы класса:
`set category(category: string)` - метод для установки заголовка категории.
`set image(src: string)`- метод для установки в разметке картинки у карточки каталога(Использует внутри метод setImage родительского класса).

####### Класс CardFull

Класс отвечает за разметку карточки с полной информации(если нажать на главной странице на любую карточку).

`constructor(container: HTMLElement,eventBroker: IEvents)` - конструктор принимает ссылку на DOM разметку с полной информации о карточке и брокер событий. 

Поля класса:
`cardButton: HTMLButton` - поле для хранения кнопки отвечающей за добавление/удаление из корзины карточки
`cardCategory: HTMLElement` - поле для хранения разметки заголовка.
`cardImage: HTMLElement` - поле для хранения разметки ценника.
`cardDescription: HTMLElement` - поле для хранения разметки с описанием карточки.
`eventBroker: IEvents` - поле для хранения ссылки брокера событий

Методы класса:
`set category(category: string)` - сеттер для установки нового заголовка.
`set description(text: string)`- сеттер для установки в разметке нового описания у карточки.
`set image(src: string)` - сеттер для установки ссылки на изображение(Использует внутри метод setImage родительского класса).
`set buttonText(text: string)` - сеттер установки текста и возможности нажатия относительно этого текста

####### Класс CardBasket

Класс отвечает за разметку карточки в корзине.

`constructor(container: HTMLElement , actions?: ICardActions)` - конструктор принимает ссылку на DOM разметку для карточки которая будет в корзине и обьект с методом который будет эмитить удаление из корзины товара по его кнопке удаления. 

Поля класса:
`cardIndex: HTMLElement` - ссылка на разметку с цифрой в списке.
`cardButtonDelete: HTMLButtonElement` - конпка удаления карточки из корзины

Методы класса:
`set index(index: number)` - сеттер меняет индекс у карточки.

####### Абстрактный Класс Form

Класс является базовым для классов связанных с формой,а именно:

1)OrderFormOne
2)OrderFormTwo

`constructor(container: HTMLElement)` - конструктор принимает ссылку на DOM. В контейнере найдём разметку для вывода ошибок.

Поля класса:
`errorsList: HTMLUListElement` - поле для разметки ошибок, которые нужно будет выводить если будут ошибки.
`buttonSumbit: HTMLButtonElement` - поле для хранения ссылки на кнопку перехода на следующую форму.(Общая у всех форм)
`form:HTMLElement` - поле для хранения ссылки на саму форму(чтобы навесить событие submit)`

Методы класса:
`set errors(errors: string[])` - сеттер для добавления ошибок в разметку.

`setToggleButton(IsDisabled: boolean)` - метод для активации/деактивации кнопки.

`abstract sumbit(): void` - абстрактный метод для того чтобы дочерние классы сами выбирали что нужно делать после перехода с данной формы.

####### Класс OrderFormOne 

Класс отвечает за разметку формы на первой странице и генераций событий при работе с пользователем.

`constructor(container: HTMLElement,eventBroker:IEvents)` - конструктор принимает ссылку на DOM формы и брокер событий. 

Поля класса:
`orderInputAddress: HTMLInputElement` - поле хранит ссылку на разметку инпута адресса.
`orderCashButton : HTMLButtonElement`- поле хранит ссылку на кнопку с оплатой наличкой.
`orderCardButton : HTMLButtonElement`- поле хранит ссылку на кнопку с оплатой картой.
`eventBroker:IEvents` - поле хранит ссылку на брокер событий.

Методы класса:

`submit(): void` - реализация абстрактного метода родительского класса Form.

`set buttonPaymentActive(value:"card"|"cash"|"")` - сеттер устанавливает класс 'button_alt-active' на нужную кнопку

####### Класс OrderFormTwo 

Класс отвечает за разметку формы на второй странице и событиями.

`constructor(container: HTMLElement,eventBroker:IEvents)` - конструктор принимает ссылку на DOM формы и брокер событий.

Поля класса:
`orderInputEmail: HTMLInputElement` - поле хранит ссылку на разметку инпута email.
`orderInputPhone: HTMLButtonElement`- поле хранит ссылку на разметку инпута телефона.
`orderButtonMakeOrder: HTMLButtonElement` - поле хранит ссылку на кнопку перехода к оплате .
`eventBroker: IEvents` - поле хранит ссылку на брокер событий

Методы класса:

`submit(): void` - реализация абстрактного метода родительского класса Form. Нужен для эмита презентеру о том что пользователь оформил заказ.

###### Описание событий происходящих в классах

P.S все события собраны в enum в начале файла main.ts и им дан ключ для централизации их.

1) Класс представления Header

Событие `click` на кнопке открытия корзины генерирует событие `headerBasket:click` передающее презентуру информацию о том что пользователь нажал по корзине

2) Класс представления Modal 

Событие `click` на кнопке(modalButtonClose) закрытия окна вызывает метода класса CloseModal()

Событие `click` на контейнере вызывает метода класса CloseModal() для закрытия по оверлею
Примечание: на модальном окне(ModalContainer) всплытие отключенно.

Событие `click` на котором обработчик отключает всплытие внутри модального окна(ModalContainer).

События `modal:open`  и `modal:close` брокера событий срабатывают при открытии модального окна и его закрытия соответственно.
Нужны для прекращения скролла при открытие и наоборот. Так же при открытии модального окна на документ навешивается событие `keydown` для отслеживания нажатия клавиши Escape и закрытии модального окна если была прожата.

3) Класс представления OrderSuccess

Событие `click` по кнопке возращения на главную страницу генерирует событие `orderSuccesButton:click` , которое даёт понять презентуру что нужно закрыть модальное окно.

4) Класс представления CardCatalog

Если в конструкторе передаётся обьект `actions` который имеет метод `onclick` на событие `click` ставится обработчик `actions.onclick`
При срабатывании этот обработчик эмитит событие `card:productSelect` и передаёт данные о товаре презентеру. 

5) Класс представления CardFull

На событие `click` у кнопки отвечающей за купить/Удалить из корзины если есть цена(иначе недоступна) вызывается в обработчике эмит события
`cardFull:toggle`.

Событие `cardFull:toggle` - передаёт презентеру информацию о пользовательском нажатии по кнопке. Презентер закрывает модальное окно с карточкой, берёт в модели каталога информацию о карточке установленной. И проверяет есть ли карточка в корзине или нет. Удаляет и добавляет из корзины относительно проверки.

6) Класс представления CardBasket

Событие `click` на кнопке удаления карточки из корзины эмитит событие `cardBasket:delete` и передаёт данные об товаре для того чтобы презентер удалил его из корзины(если он есть там)

7) Класс представления OrderFormOne

На кнопки(orderCardButton и orderCashButton) выбора оплаты ставятся обработчика события `click` которые генерируют события `payment:new` и передают в обьекте в поле payment выбор пользователя('cash' или 'card')

На инпуте адресса стоит обработчик события `input` который при изменении ввода генерирует событие и передаёт данные о вводе.

На форму навешено событие `submit` для того чтобы срабатывал он не только с нажатия кнопки, а так же с Enter в инпуте например.

8) Класс представления OrderFormTwo

Событие `input` на инпуте email и Phone обработчиком генерирует событие `email:new` и передаёт презентеру информацию о вводимом тексте.

На форму навешено событие `submit` для того чтобы срабатывал он не только с нажатия кнопки, а так же с Enter в инпуте например.

Событие `orderFormTwo:makeOrder` в презентере отправляет данные на сервер и очищает корзину и информацию о пользователе.

9) Класс модели Buyer(телефон,адресс,емэйл,выбор способа оплаты)

При любом изменении данных модель эмитит об этом презентера. Презентер с помощью прослушивания события `orderFormOneModel:changed` думает заполнил ли правильно пользователь форму

10) Класс модели Catalog

При любом измении каталога или установлении продукта(для отдельного просмотра полной информации о нём) испускаются нужные события

a) `card:setProductCard` - перерисовка карточки с полной информацией
b) `сatalog:changed` - перерисовка презентером главную страницу с карточками

11) Класс модели ShoppingBasket

При любом изменении модели генерируется событие `basket:changed` и презентер перерисовывет список товаров в корзине

###### Презентер

Отвечает за логику работы приложения. Написан в файле main.ts

Описание каждого слушателя события:

`сatalog:changed` - срабатывает при изменение модели данных Каталога. Получает с модели данных актуальные товары и отрисовывает с помощью класса представления CardCatalog каждую карточку отдельно. После этого все карточки отрисовываются на главной страницу с помощью класс представления CatalogView.

`card:productSelect` - срабатывает при клике на карточку в галерии пользователем,презентер устанавливает карточку в моделях данных каталога в поле _productCard.


`card:setProductCard` - (продолжение события `card:productSelect`) срабатывает если карточка просмотра в модели данных каталога изменилась.
Тут презентер думает как отрисовать на кнопке текст в зависимости от цены и относительно проверки корзины на наличие этой карточки.

`headerBasket:click` - срабатывает когда пользователь нажал  на корзину в шапке. Идёт получение актульных данных с модели данных корзины о карточках в ней и далее идёт отрисовка этих карточек(CardBasket),там же идёт передача генерации события удаления карточки в каждый инстанс класс CardBasket. После этого идёт проверка на то есть ли вообще товары в корзине и если нет то кнопка в корзине перехода к данных формы недоступна. 

`cardFull:toggle` - при нажатии(класс представления уведомит) на кнопку "Купить" или "Удалить из корзины" презентер проверяет есть ли товар в корзине и добавляет/удаляет товар из/в корзины.

`basket:changed` - перерисовка корзины презентером при получении эмита события из модели данных корзины.Так же идёт перерисовка кол-ва товаров на иконке в шапке

`cardBasket:delete` - при срабатывании эмита происходит удаление презентером товара из корзины с проверкой есть ли этот товар в корзине.

`basketMakeOrder:click` - при срабатывание эмита из модального окна с контентом корзины происходит переход к первой части формы.

`payment:new` - если пользователь нажмёт на кнопку выбора способа оплаты,класс представления уведомит и презентер вызовет модель данных покупателя чтобы присвоить значение в нужно поле.

`address:new` - если пользователь введёт адресс,класс представления уведомит и презентер вызовет модель данных покупателя чтобы присвоить значение в нужно поле.

`orderFormOneModel:changed` - если в первой части формы представления пользователь меняет способ оплаты или вводит что в инпут адресса презентер реагирует и даёт команду отрисововать ошибки если они есть.

`orderFormOne:next` - при submit,презентер отрисовывает вторую часть формы.

`email:new` - при срабатывании устанавливает новый емэйл в модели данных покупателя.

`phone:new` - при срабатывании устанавливает новый телефон в модели данных покупателя.

`orderFormTwoModel:changed` - проверяет при изменения данных на второй форме всё введено и даёт команду отрисовать ошибки если нет.

`orderFormTwo:makeOrder` - презентер при срабатывании готовит для отправки на сервер информацию о покупке и отрисовывает модальное окно с информацией о списанной сумме.Затем очищает товары в корзине и информацию о пользователе.

`orderSuccesButton:click` - презентер закрывает окно с успешной покупкой при клике на кнопку вернуться к покупкам.

`modal:open` - при открытии модального окна на документ вешается overflow со значением hidden.

`modal:close` - при закрытии модального окна на документ вешается overflow со значением visible.
